# SilkPlot workspace settings
# Keep the dependency tree flat and deterministic for the monorepo.
save-exact=false
engine-strict=true
# Public packages publish under the @silkplot scope.
# access=public
