{
  "$schema": "https://json.schemastore.org/tsconfig",
  // See `packages/core/tsconfig.test.json` for why `test/` needs its own config
  // and why `src` is included alongside it.
  //
  // Node suites stay Temporal-only. The week-grid browser suite is TSX and
  // needs the DOM lib the package tsconfig now carries for Solid.
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "composite": false,
    "declaration": false,
    "declarationMap": false,
    "sourceMap": false,
    "types": []
  },
  "include": ["src/**/*.ts", "src/**/*.tsx", "test/**/*.ts", "test/**/*.tsx"]
}
